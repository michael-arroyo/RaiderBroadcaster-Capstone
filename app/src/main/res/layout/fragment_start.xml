<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50.0dip"
        android:gravity="center"
        android:text="Enter a bus name:"
        android:textColor="@color/pn_blue"
        android:textSize="35.0sp" />

    <EditText
        android:id="@+id/input_busname"
        android:layout_width="fill_parent"
        android:layout_height="75.0dip"
        android:layout_below="@id/textView"
        android:gravity="center"
        android:hint="Bus name"
        android:inputType="text"
        android:textColor="#ff333333"
        android:textSize="30.0sp" />

    <Spinner
        android:id="@+id/input_bustype"
        android:layout_width="fill_parent"
        android:layout_height="75.0dip"
        android:layout_below="@id/input_busname"
        android:entries="@array/busTypes"
        android:gravity="center"
        android:prompt="@string/busTypePrompt" />

    <Button
        android:id="@+id/btn_start_broadcast"
        android:layout_width="fill_parent"
        android:layout_height="100.0dip"
        android:layout_centerInParent="true"
        android:background="#ff41b32b"
        android:text="Broadcast"
        android:textSize="25.0sp" />
</RelativeLayout>