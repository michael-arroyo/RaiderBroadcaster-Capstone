<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.test.raiderbroadcaster.StopFragment">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_stop_broadcast"
        android:layout_alignParentLeft="true"
        android:layout_marginBottom="50dp"
        android:gravity="center"
        android:text="Bus ID: "
        android:textColor="@color/pn_blue"
        android:textSize="35sp" />

    <TextView
        android:id="@+id/text_busname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_stop_broadcast"
        android:layout_marginBottom="50dp"
        android:layout_toRightOf="@id/textView"
        android:textSize="35sp" />

    <Button
        android:id="@+id/btn_stop_broadcast"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_centerInParent="true"
        android:background="@color/pn_red"
        android:text="Stop Broadcast"
        android:textSize="25sp" />

</RelativeLayout>